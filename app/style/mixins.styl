/**
*
* app/style/mixins.styl
* Mixins
*
**/

// CENTER
center() {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
}
centerX() {
  position: absolute;
  left: 50%;
  transform: translateX(-50%);
}
centerY() {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
}

// CONTAINER
container() {
  width: 90%;
  max-width: 1024px;
  min-height: 100%;
  margin: auto;
}

// OVERLAY
overlay() {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  opacity: 0;
  background-color: overlayColor;
  transition: 0.2s;
    &:hover {
      opacity: 1;
    }
}

// icon
icon(size = _margin, y = 0) {
  width: size;
  height: size;
  background-size: size size*9;  // NBR OF ICONS
  background-repeat: no-repeat;
  background-position-y: -(y*size);
  background-image: url('../'+imgsPath+'icons.svg');
}

// MEDIA QUERIES
mobile() {
  @media screen and (max-device-width: 480px) {
    {block}
  }
}
desktop() {
  @media screen and (min-width: 1024px){
    {block}
  }
}
mediaqueries(maxWidth) {
  @media screen and (max-width: maxWidth){
    {block}
  }
}

// CIRCLE
circle(d) {
  width: d;
  height: d;
  border-radius: d;
}
